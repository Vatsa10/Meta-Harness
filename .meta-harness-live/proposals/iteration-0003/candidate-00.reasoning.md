The iteration-0002-00 harness failed on task #13 (predicting "invoice_error" instead of the correct label) by using an ultra-minimal prompt "Label this: {query}\nOptions: {options}\nAnswer:" which saves tokens but loses semantic clarity. The model may not understand "Label this" as clearly as "Classify". The winning baselines (1.000 score) use slightly more verbose prompts that explicitly say "Classify the input" and "Return exactly one valid label", providing clearer instruction framing. I'll create a harness that balances token efficiency with instruction clarity by using "Classify:" as a middle ground—shorter than "Classify the input" but more semantically explicit than "Label this". This would be refuted if the new harness scores below 1.000 or uses more tokens than the 785-token winners.