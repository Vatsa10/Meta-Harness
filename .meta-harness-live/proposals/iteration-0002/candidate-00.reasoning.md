The evidence shows that all baselines achieve near-perfect scores with minimal context cost, indicating the task set is straightforward. The only failure is initial-0003 predicting "documentation" on task #20, likely due to its more complex prompt asking the model to "pick the most relevant skill from the library" which adds cognitive overhead even when the library is empty. The winning approaches use direct, minimal prompts. I'll propose a harness that optimizes for the lowest possible context cost by using an even more concise prompt format while maintaining the zero-shot baseline's perfect accuracy. Instead of "Classify the input.\nValid labels: X\n\nInput: Y\nReturn exactly one valid label.\nLabel:", I'll use "Label this: Y\nOptions: X\nAnswer:" which reduces boilerplate tokens. This would be refuted if the shorter prompt causes the model to produce unparseable outputs or miss labels due to insufficient instruction clarity.